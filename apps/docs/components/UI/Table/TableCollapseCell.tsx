import { styled } from '@mui/material/styles'
import TableCell from '@mui/material/TableCell'

const TableCollapseCell = styled(TableCell)({
  padding: '0 0 0 48px',
  borderBottom: 0,
})

export default TableCollapseCell
